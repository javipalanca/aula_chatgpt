VITE_OLLAMA_URL=
VITE_OLLAMA_MODEL=llama2

# Optional: Local proxy server for persistence (Express + MongoDB).
# If you run the included proxy, set VITE_STORAGE_API to point to it.
# Example: VITE_STORAGE_API=http://localhost:4000
VITE_STORAGE_API=

# If you use the supplied proxy, configure the server with MONGO_URI and MONGO_DB
# Example MONGO_URI: mongodb://gtirouter.dsic.upv.es:43012
# Example MONGO_DB: aula_chatgpt
MONGO_URI=
MONGO_DB=

# Password for teacher mode (set to enable the teacher dashboard)
# Example: VITE_TEACHER_PASSWORD=mi-contrasegna-secreta
VITE_TEACHER_PASSWORD=

# Copia este archivo a .env y ajusta la URL de tu servidor Ollama
# Ejemplo: VITE_OLLAMA_URL=http://localhost:11434
VITE_OLLAMA_URL=
# Modelo por defecto a usar en Ollama (opcional)
VITE_OLLAMA_MODEL=llama2
